
matrixA = [[1, 2, 3], [1, 2, 3]]
matrixB = [[5, 6, 7], [5, 6, 7], [5, 6, 7]]
matrixC = [[8, 9], [8, 9], [8, 9], [8, 9]]
matrixD = [[10, 11], [10, 11], [10, 11]]

def matrix_multiplication(matrix1, matrix2):
    
    #<------------INSERT CODE HERE------------>

        matrix1rows = len(matrix1)
        matrix1cols = len(matrix1[0])
        matrix2rows = len(matrix2)
        matrix2cols = len(matrix2[0])

        print("Matrix one rows: %i, Matrix one cols: %i" % (matrix1rows, matrix1cols))
        print("Matrix two rows: %i, Matrix two cols: %i" % (matrix2rows, matrix2cols))
        
        multmatrix = []
        return 0

matrix_multiplication(matrixC, matrixB)
    

